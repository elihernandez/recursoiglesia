import Head from 'next/head'

export default function LoginPage(props) {
    return (
        <>
            <Head>
                <title>Iniciar sesión</title>
                <meta name="description" content="Iniciar sesión en Recursoiglesia" />
            </Head>
        </>
    )
}